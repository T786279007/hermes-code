# Hermes Agent 状态对照清单

以下对照表可直接复制回飞书，用于让读者分辨文档中“已完成”说法和当前主仓库事实之间的差异，方便集中修正文档描述而不是代码。表格三列依次为：飞书文档里的表述（引用原 doc 断言）、实际代码里可观察到的事实、以及建议的改写方向。

| 文档说法 | 当前代码事实 | 建议改写 |
| --- | --- | --- |
| 第 10/11/16 节提到 `.progress -> 注册表 -> Cron/Zoe` 链路已打通，并且注册表里同步了 `tool_count/files_written/last_event/checks` 等实时指标。 | `claude_runner.py` 和 `codex_runner.py` 都是同步执行（`subprocess.communicate`）的阻塞 runner，`executor.finish_task` 只有在任务完成时一次性写入 `stderr_tail`、`result`、可选的 `done_checks_json` 等字段；主线代码里没有 `.progress` 事件流，唯一的 `stream-json`+`.progress` 实现还在 `hermes-agent/claude-task-runner.py` 之外。 | 说明主系统目前只在完成时更新注册表，`.progress`/事件流仍属实验/旁路代码，Cron 看到的指标来自终态 `done_checks_json`，不是实时工具计数。 |
| 文档表示注册表收集了 prompt、tool_count、last_event 等结构化数据并可直接查询。 | `task_registry.py` 的 schema 现在定义了 `prompt`、`result`、`done_checks_json` 等字段，初始化阶段会自动为遗留 DB 补齐 `done_checks_json` 列，`finish_task` 侧写入时会把 `run_done_checks` 的 JSON 结果写到该列；其余字段还停留在默认日志/状态，不存在 `tool_count` 这类列。 | 需要把文档改成“注册表记录 prompt/result/完成检查对象，且新部署会自动添加 `done_checks_json`”，避免让人误以为已有全量进度列。 |
| “Cron 每 6 项检查 + 自动重试/PR/CI/审查状态”已经上线，`needs_attention` 会发出告警。 | `check_agents.HealthChecker` 目前返回：系统磁盘、任务数量履历、运行 agent 的 PID/存活状态、数据库健康、基于 `_task_timeout` 的 stale 任务列表、通过 `gh pr list` 查询的 open PR + CI 失败标识，`needs_attention` 仅在 stale 或 CI 失败时置真。 | 建议写明 Cron 报告的确切维度（disk/tasks/agents/database/stale/pr_status/needs_attention），以及 `needs_attention` 是 stale 或 CI 失败的派生布尔值，而不是“自动识别所有审查状态”。 |
| “完成定义”部分的 PR/CI/截图检查已全部在注册表里可追踪。 | `done_checker.run_done_checks` 依次执行 commit/tests/PR/CI/截图验证，`executor` 只会在成功提交后调用一次，拿到的 `pr_number`、`ci_passed`、`screenshot_included` 等字段用 `json.dumps` 写进 `done_checks_json`（现在也能安全写入旧 DB，因为 `TaskRegistry` 自动添加列）。 | 把文档修成“完成后会运行一次 done_defs 并把结果写入 `done_checks_json`”，强调它是终态检查而非持续监控，并指出字段已经存在表中便于查询。 |
| “代码审查模块”段落说 inline comment 直接呼 `repos/{owner}/{repo}/pulls/...`，应该永远成功。 | `review_pr.post_inline_comment` 现在首先调用 `/repos/{owner}/{repo}/pulls/<n>/comments`，如果 `_run_command` 抛 `CLIError` 就在 catch 里写日志并立即重试 `gh api pulls/<n>/comments`，真实网络/认证错误能触发 fallback；相关测试覆盖了“第一次失败第二次成功”的情况。 | 建议把这个容错流程写进文档，让人知道当 GH CLI 推断 owner/repo 失败时仍有备选相对路径。 |
| “Codex 集成已完成”语义里没有强调超长运行。 | `executor` 会把 Codex 任务交给 `CodexRunner`，该 runner 调用 `codex exec --model gpt-5.4` 并基于 `CODEX_TIMEOUT`（当前为 21600s）设置计时器；`reconciler` 与 `HealthChecker` 的 `_task_timeout` 函数会针对 `agent == "codex"` 返回 `CODEX_TIMEOUT * 2`，表示 Codex 任务视作需要 12h 才会被判 stale。 | 把文档补充“Codex 任务允许 6 小时基本运行、重试/监控逻辑会在 12 小时后才判 stale”，以免读者误解跟 claude/其它 agent 一样短。 |
| “实验结果”章节声称 10/10 实验全部通过。 | `python3 -m pytest -q` 在主仓库得到 `330 passed`（含 task_registry、reconciler、check_agents、review_pr 等模块的回归测试），所有关键风险点都有针对性测试覆盖。 | 改成“完整 pytest 套件（330 个测试）通绿，重点实验分别由 tests/test_task_registry.py、tests/test_reconciler.py、tests/test_check_agents.py、tests/test_review_pr.py 等保障”，避免把“实验”说成离线 QA 记录。 |
| “timeout/迁移修复”部分提到 `CODEX_TIMEOUT` 和 `done_checks` 改动已经落实。 | `config.py` 定义了 `CLAUDE_TIMEOUT=300`、`CODEX_TIMEOUT=21600`、`RECONCILER_TIMEOUT=600`；`reconciler` 与 `HealthChecker` 均通过 `_task_timeout` 根据 agent 类别返回 `CLAUDE_TIMEOUT*2`/`CODEX_TIMEOUT*2`/`RECONCILER_TIMEOUT`，并在判定 stale 后调用 `TaskRegistry.finish_task`；`TaskRegistry` 在初始化时检查 `PRAGMA table_info(tasks)`，若缺 `done_checks_json` 会执行 `ALTER TABLE` 以避免旧数据库崩溃。 | 建议把这段改成“系统每次启动都会自动补齐 `done_checks_json`，reconciler/health_checker 依据 agent-specific timeout（Claude 300s、Codex 21600s、其它 600s）来识别 stale”，明确常量和行为。 |

